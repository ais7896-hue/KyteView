# KyteView 安裝檔建立前完整指令指南

本指南提供將 **KyteView** 從 Python 原始碼打包為**無黑窗獨立執行檔 (`KyteView.exe`)**，並進一步封裝成**專業 Windows 安裝精靈 (`KyteView_Setup_1.0.0.exe`)** 的完整步驟與指令。

---

## 📋 步驟零：環境確認與依賴準備

開啟 PowerShell，進入專案根目錄並啟用虛擬環境：

```powershell
cd "d:\Noah\Antigravity專案程式專用\KyteView"
.venv\Scripts\activate
```

安裝最新版打包套件與確認專案依賴：

```powershell
pip install --upgrade pyinstaller
pip install -r requirements.txt
```

---

## 📦 步驟一：使用 PyInstaller 打包獨立程式

> **推薦方案：目錄模式 (`--onedir`)**
> 
> 對於像 KyteView 這類要求「按下 Space 瞬間彈出、零秒延遲」的常駐工具，**強烈推薦採用目錄模式 (`--onedir`)**。
> - **原因**：單檔版 (`--onefile`) 每次啟動時都要將數十 MB 的動態庫解壓縮到 `%TEMP%`，會造成 1~2 秒的初始卡頓；而目錄模式解壓縮僅在安裝時執行一次，日常使用能在 50ms 內毫秒級秒開。

執行以下指令進行乾淨編譯打包：

```powershell
# 清除過往建置暫存
Remove-Item -Recurse -Force build, dist -ErrorAction SilentlyContinue

# 執行 PyInstaller 打包
pyinstaller --noconfirm --onedir --windowed `
    --name "KyteView" `
    --icon "assets/icon.ico" `
    --add-data "assets;assets" `
    --hidden-import "py7zr" `
    --hidden-import "mammoth" `
    --hidden-import "pptx" `
    --hidden-import "fitz" `
    --hidden-import "pymupdf" `
    --hidden-import "miniaudio" `
    --hidden-import "calamine" `
    --hidden-import "python_calamine" `
    --hidden-import "pygments" `
    --hidden-import "markdown_it" `
    --hidden-import "win32timezone" `
    main.py
```

> **備案：若需要單一可執行檔 (`--onefile`)**
> 將上述指令中的 `--onedir` 替換為 `--onefile` 即可。

---

## 🧪 步驟二：本機測試驗證

打包完成後，先手動執行產出目錄中的程式，確保沒有遺漏任何動態庫或相依模組：

```powershell
.\dist\KyteView\KyteView.exe
```

- **驗證項目**：
  1. 程式是否正常常駐於背景，系統托盤或工作管理員是否有 KyteView 處理程序。
  2. 隨意在檔案總管選取 `.png`, `.mp4`, `.docx`, `.pptx`, `.zip`, `.py`，按下 `Space` 是否能正常秒開預覽。
  3. 驗證完成後於預覽視窗按 `ESC` 關閉，並由托盤或工作管理員結束測試程序。

---

## 💿 步驟三：使用 Inno Setup 編譯安裝檔精靈

確保電腦已安裝 [Inno Setup 6](https://jrsoftware.org/isdl.php)（預設安裝路徑通常為 `C:\Program Files (x86)\Inno Setup 6\`）。

直接在命令列執行 Inno Setup 編譯器指令：

```powershell
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" setup.iss
```

編譯完成後，安裝檔將產生於：
```
dist\KyteView_Setup_1.0.0.exe
```

---

## ⚡ 步驟四：一鍵自動化打包腳本 (推薦)

您可以將打包與安裝包編譯整合成單一自動化指令。在專案根目錄下建立 `build_installer.ps1`，未來只需右鍵「使用 PowerShell 執行」即可全自動完成：

```powershell
# build_installer.ps1
Write-Host ">>> [1/3] 正在清理過往建置產物..." -ForegroundColor Cyan
Remove-Item -Recurse -Force build, dist -ErrorAction SilentlyContinue

Write-Host ">>> [2/3] 正在執行 PyInstaller 編譯..." -ForegroundColor Cyan
pyinstaller --noconfirm --onedir --windowed `
    --name "KyteView" `
    --icon "assets/icon.ico" `
    --add-data "assets;assets" `
    --hidden-import "py7zr" `
    --hidden-import "mammoth" `
    --hidden-import "pptx" `
    --hidden-import "fitz" `
    --hidden-import "pymupdf" `
    --hidden-import "miniaudio" `
    --hidden-import "calamine" `
    --hidden-import "python_calamine" `
    --hidden-import "pygments" `
    --hidden-import "markdown_it" `
    --hidden-import "win32timezone" `
    main.py

if ($LASTEXITCODE -ne 0) {
    Write-Host "[ERROR] PyInstaller 編譯失敗，請檢查上方錯誤訊息！" -ForegroundColor Red
    exit 1
}

Write-Host ">>> [3/3] 正在使用 Inno Setup 封裝安裝檔..." -ForegroundColor Cyan
$isccPath = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
if (Test-Path $isccPath) {
    & $isccPath setup.iss
    Write-Host ">>> [SUCCESS] KyteView 安裝程式打包成功！請至 dist 目錄查看 KyteView_Setup_1.0.0.exe" -ForegroundColor Green
} else {
    Write-Host "[WARNING] 未偵測到 Inno Setup 6 編譯器 ($isccPath)。" -ForegroundColor Yellow
    Write-Host "可直接散發 dist\KyteView 目錄，或安裝 Inno Setup 後手動編譯 setup.iss。" -ForegroundColor Yellow
}
```

---

## ⚠️ 重要發布注意事項

1. **排除個人設定檔案**：
   - 確保 `dist\KyteView\` 中**不要夾帶**本機測試用的 `config.json`，讓安裝後的使用者自動以乾淨的預設值初始化。
2. **防毒軟體 / Windows SmartScreen**：
   - 未經數位簽名（Code Signing Certificate）的新編譯 EXE 在首次下載執行時可能會觸發 Windows Defender SmartScreen 提示，點擊「其他資訊 $\rightarrow$ 仍要執行」即可，或使用開源工具（如 SignPath）對 EXE 進行數位簽章。
